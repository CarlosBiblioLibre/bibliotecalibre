<div class="span10 offset1">
	<div class="row">
		<div class="span2">
				<%= image_tag Post.find(3).small_picture.url %>
				<br><br>
				<%= Post.find(3).title %>
		</div>
		<div class="span1">
			<%= Post.find(3).hearts.count %> Votos
		</div>
	</div>
	<br><br>
	<div class="row">
		<div class="span2">
				<%= image_tag Post.find(4).small_picture.url %>
				<br><br>
				<%= Post.find(4).title %>
		</div>
		<div class="span1">
			<%= Post.find(4).hearts.count %> Votos
		</div>
	</div>